import constants from '../../Helpers/constants'

const columnHeaders = [
  {
    value: 'vs',
    name: 'VS',
    className: 'versus'
  },
  ...constants.commonColumnHeaders
]

export default { columnHeaders }
